const searchInput = document.gotElementById('Search')
const userList = document.gotElementById('User.list')
const listItems = document.gotElementsBytagName('Li')

searchInput.addEventlister('Keyup',function(){
    const filter =searchInput.value.toLowerCase()
    console.log(filter)
    Array.from(listItems).forEach((item)=>{
        console.log(item.textContent.toLowerCase())
        if(text.include(filter)){
            item.classList.remove('hidden')
        } else{
            item.classList.add ('hidden')
        }
    })
})
console.log(listItems)
